# Wove examples

Run `cargo run -p wove-examples --bin editor` for multiline editing, styled text,
and a live status line. This workspace package is not published.

Small API examples remain beside their libraries:

```sh
cargo run -p wove --example gallery
cargo run -p wove-dioxus --example counter
```
