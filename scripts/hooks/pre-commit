#!/bin/sh
# Preserve existing checks, then inspect staged content without touching the worktree.
set -eu
previous=$(git config --get wove.previousPreCommit || true)
if [ -n "$previous" ] && [ -x "$previous" ]; then
  "$previous" "$@"
fi
exec python3 scripts/hooks/check.py
